## 08:19 〜 Go 標準ライブラリ再実装の方針を決めた

### やったこと
- 現行 bun CLI はファイル走査・`git` / `gh`・JSON・表出力だけなので、第三者パッケージなしで移植できると判断した
- 姉妹リポジトリ `wht-get-ready-for-github`（`grg`）と同じ制約（stdlib のみ、`src/` / `dist/`、darwin/arm64、`make release`）に揃えることにした
- ADR-0001〜0006 を実装前に書いた

### 関連ADR / コミット
- ADR-0001（Superseded）
- ADR-0002
- ADR-0003
- ADR-0004
- ADR-0005
- ADR-0006

## 08:30 〜 Go CLI を実装して bun ソースを削除した

### やったこと
- `src/` に走査・状態判定・表出力・`--dir` / `--help` / `--version` を置いた
- リポジトリ間は同時 8 件、同一リポジトリでは `git fetch` のあと `rev-list` するようにした
- bun の `wht-ls-github.ts` を削除した
- README / DEVELOPING / Makefile をバイナリ配布向けに直した

### 関連ADR / コミット
- ADR-0002
- ADR-0003
- ADR-0004
- ADR-0005
- ADR-0006

## 09:01 〜 make の既定ビルドと版数操作を grg に揃えた

### やったこと
- `make` / `make build` をホスト向けにした。作ったバイナリをその場で `--version` できる
- `make show-version` / `make version` と `make release` / `release-minor` / `release-major` を grg と同じ流れにした
- リリース成果物だけ darwin/arm64 のままにした
- ADR-0007 を書いた

### 関連ADR / コミット
- ADR-0003（0007 で改正）
- ADR-0007

## 09:05 〜 README と development.md を grg と同じ役割に分けた

### やったこと
- トップの README に概要・前提・入れ方・使い方・表示すること・やらないことを書いた
- ビルドとリリースは `development.md` に移し、`DEVELOPING.md` は置かないことにした

### 関連ADR / コミット
- ADR-0008

## 09:30 〜 GitHub Actions からリリースできるようにした

### やったこと
- Web で編集したあと Actions の Run workflow で patch / minor / major を上げ、darwin/arm64 を Releases に載せる方針を ADR-0009 に書いた
- `.github/workflows/release.yml` を追加した。公式 Action だけ使う
- CI では `git push origin HEAD` を飛ばす `make ci-release` を足した

### 関連ADR / コミット
- ADR-0009
